spring:
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema.sql

app:
  user:
    nickname:
      adjective:
        - 차분한
      color:
        - 보라
